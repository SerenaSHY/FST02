- Configure git
git config --global user.name "username"
git config --global user.email "email"
git config user.name //check

-check status
git init //initialize the master
git status

-Git 3 Stage Architecture
-Add modified file(s) in the staging Area
git add .
. mean to move all the changes into the stagging stage

-Create a Commitgit commit -m "This is the description"
m is for message

-Check the history 
git log 
git log --oneline
exit log: only "q" in the log and enter

-Branching

-Check all of the branch
git branch -a

-Create a new branch
git branch [branch_name]

-Switch to a different branch
git checkout [branch_name]
git switch [branch_name]

M - modified (orange)
U - Untracked (Green)